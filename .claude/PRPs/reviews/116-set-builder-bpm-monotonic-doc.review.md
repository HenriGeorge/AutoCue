# Self-review — Issue #116 fix

## Verdict

**Approve.**

## Scope

Doc-only change to `docs/reference/set-builder.md` (+ investigation
artifact). No Python, JS, HTML, or test code touched. Diff is 49 added
lines / 1 deleted line in the doc, well under the 50-line guideline.

## Issues found

None.

Audit dimensions:

- **Correctness vs source:**
  - The inequality `current_bpm × 0.97 ≤ cand_bpm ≤ current_bpm × (1 +
    bpm_step_max)` matches `autocue/analysis/setbuilder.py:447–449`
    (ascending branch).
  - The worked example (120→128, dip from 118.81→117.65) reproduces the
    issue reporter's exact probe output: `117.65 ≥ 118.81 × 0.97 =
    115.24` checks out arithmetically.
  - The seed-pass-two callout matches `setbuilder.py:403`
    (`min_bpm in (start_bpm * 0.97, 0.0)`).
  - The reweighting hint (BPM weight 0.25 instead of 0.40) matches
    `setbuilder.py:233`.
  - The anchor workaround is real: `_merge_anchors` at
    `setbuilder.py:330` inserts anchors at BPM-sorted positions
    regardless of the gate.

- **TOC integrity:**
  Added `- [BPM monotonicity](#bpm-monotonicity)` under
  `## 6. Candidate retrieval`. Existing anchors
  `#asymmetric-bpm-gate`, `#4-seed-selection--_find_seed`,
  `#9-bpm-progress-bonus`,
  `#8-setbuilder-specific-transition-reweighting`,
  `#12-anchor-merging--_merge_anchors` were all referenced from the
  new content and remain unchanged.

- **No silent feature removal:**
  Nothing was deleted from `docs/qa_tester.md`'s documented feature
  sweep. The doc was *expanded*, not contracted.

- **Hard-rule compliance:**
  No master.db touched, no CORS change, no hook bypass, no `git push
  --force`, no scope creep beyond the issue's described gap.

## Verification

- **Leg A (`pytest -x -q`):** 1325 passed, 4 skipped, 1 warning, 16.13s.
  Doc-only change cannot break tests; ran the full suite as the
  first-iteration baseline.
- **Leg B (`npm test --silent`):** 564 passed across 28 test files, 2.39s.
- **Leg C (`cd tests/e2e && npm test`):** **pre-existing failure**
  `Error: test file "per-control-sweep.selector.test.ts" should not
  import test file "per-control-sweep.spec.ts"`. Confirmed by stashing
  the diff (`git stash push -- docs/reference/set-builder.md`) and
  re-running — same error. Filed under `tests/e2e/` infrastructure,
  not in any path my diff touches. Not blocking for this PR (would
  block every PR that doesn't touch `tests/e2e/` until separately
  fixed). Recommend filing a separate issue against the e2e harness.

## Counter-tests

If the fix is reverted (doc rolled back to the original
"asymmetric — see Candidate retrieval" wording), the issue reporter's
exact complaint resurfaces: the doc would again imply monotonic
behavior in `build` mode and not call out the head-track relaxation.
No code test can guard a doc-only change; the regression guard here is
the issue itself.

## Risks

- TOC anchor `#bpm-monotonicity` is auto-generated by GitHub's markdown
  renderer from the `### BPM monotonicity` heading. Verified by
  inspection that no other heading in the file collides with that
  slug.
- No risk to production runtime, API surface, or storage.

## Out-of-scope follow-ups (not done here)

1. The pre-existing e2e import error in
   `tests/e2e/per-control-sweep.selector.test.ts` deserves its own
   issue. Per agent rules (scope ≤ 50 lines, fix only what the issue
   describes), did not touch it.
2. `tests/e2e/package-lock.json` was created by `npm install` during
   verification; **not staged** to keep the diff scope tight. Left
   untracked in the worktree — workflow cleanup will discard it.
