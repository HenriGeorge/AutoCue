# cc-worktrees per-repo config
FIGMA_LAUNCH=1                     # open the project Figma file on create
FIGMA_FILE_KEY="BuHYH2fbxC4UCNlJCj5oCn"   # Figma file "AutoCue" (named after this repo)
# CRON — schedule launchd/cron to run claude -p (or a daemon) in a dedicated self-syncing worktree
# pinned to origin/main, so a scheduled job always runs MERGED main code (bin/cron-run.sh, docs/CRON.md).
# CRON_ENABLE=1                 # opt in bin/cron-run.sh; OFF by default — a billed/standing run is never a default
