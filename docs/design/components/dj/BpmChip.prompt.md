**BpmChip / KeyChip** — the two mono data pills on every track row. `BpmChip` is green-washed and shows one decimal; `KeyChip` is neutral surface-2.

```jsx
<BpmChip bpm={124.5} />     {/* 124.5 BPM, green */}
<KeyChip keyName="8A" />    {/* neutral pill */}
```
