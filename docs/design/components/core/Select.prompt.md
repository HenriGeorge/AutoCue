**Select** — native dropdown matching the input style (8px radius, green focus ring), optional stacked label.

```jsx
<Select label="Energy mode" options={['Build (ascending)', 'Drop (descending)', 'Flat']} />
<Select options={[{value:'warmup',label:'Warm-up'},{value:'peak',label:'Peak'}]} />
```
