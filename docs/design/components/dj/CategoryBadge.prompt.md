**CategoryBadge** — the DJ-set category chip. Five categories, each with its own hue: warmup (green), build (blue), peak (red), after-hours (purple), closing (orange).

```jsx
<CategoryBadge category="peak" />
<CategoryBadge category="warmup" />
```
